using UnityEngine;

public enum CoffeeBuilding
{
    ChoosingCup,
    ChoosingCoffeeType,
    AddingWhipCream
}
public class CoffeeBuilder : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void StartBuildingCoffee()
    {
        //starts the coffee building process
    }

    void FinishBuildingCoffee()
    {
        //finishes the coffee building process
    }

    void RestartBuildingCoffee()
    {
        //restarts the coffee building process
    }
}
