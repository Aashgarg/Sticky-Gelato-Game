using UnityEngine;
public enum GelatoBuilding
{
    PickingContainer,
    ChoosingFlavor,
    AddingToppings
}
public class GelatoBuilder : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void StartBuildingGelato()
    {
        //starts the gelato building process
    }

    void FinishBuildingGelato()
    {
        //finishes the gelato building process
    }

    void RestartBuildingGelato()
    {
        //restarts the gelato building process
    }

}
